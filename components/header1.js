import { AppBar, Toolbar, Typography } from "@material-ui/core";
import React from "react";

const header1 = () => {

    return(
        <AppBar>
            <Toolbar>
                <Typography>
                    this is our header
                </Typography>
            </Toolbar>
        </AppBar>

    );

};
export default header1;